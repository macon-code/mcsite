//DOMContentLoaded ensures that anything in the function occurs after the html elements
//are loaded
document.addEventListener('DOMContentLoaded', function() {

    // Just a generic function for future reference
    function handleIndicatorClick(param) {
        // Your code to execute when the button is clicked
        console.log(param);
    }

});